# ==========================================
# CoPro Connect - Variables d'environnement
# ==========================================
# Copier ce fichier en .env et remplir les valeurs.
#   cp .env.example .env
#
# Ces variables sont lues par application.properties via ${VAR:default}.

# JWT - Générer une clé : openssl rand -base64 64
JWT_SECRET=

# MongoDB
MONGODB_URI=mongodb://localhost:27017/copro-connect

# Mail (Brevo SMTP)
MAIL_HOST=smtp-relay.brevo.com
MAIL_PORT=587
MAIL_USERNAME=
MAIL_PASSWORD=

# MFA - Adresse email expéditeur
MFA_SENDER_EMAIL=noreply@copro-connect.fr

# CORS - Origines autorisées (séparées par des virgules)
CORS_ORIGINS=http://localhost:3000,http://localhost:4200
